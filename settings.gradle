// The project name.
// -- Because gradle by default, uses the checked out directory as the project name. On jenkins, this turns out to be "workspace".
rootProject.name='ads-proto'
include 'client'
include 'server'
