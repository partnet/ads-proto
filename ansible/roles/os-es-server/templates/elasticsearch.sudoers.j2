%{{CFG_GROUP}} ALL = (ALL) NOPASSWD: /sbin/service elasticsearch-{{CFG_USER}} start, /sbin/service elasticsearch-{{CFG_USER}} stop, /sbin/service elasticsearch-{{CFG_USER}} restart, /sbin/service elasticsearch-{{CFG_USER}} status
